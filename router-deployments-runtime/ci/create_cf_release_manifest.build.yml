---
platform: linux

image: docker:///cloudfoundry/runtime-ci

inputs:
  - name: cf-release-rc
  - name: cf-routing-release
  - name: deployments-routing

run:
  path: cf-routing-release/router-deployments-runtime/ci/create_cf_release_manifest

params:
  INFRASTRUCTURE:
  ENVIRONMENT:
