#!/bin/bash -e

host -r <%= p("dns_health_check_host") %>
if [[ "0" != "$?" ]]; then
  echo "DNS is not up"
  exit 1
fi

cp /var/vcap/jobs/tcp_router/config/haproxy.conf /var/vcap/data/tcp_router/config/
chown vcap:vcap /var/vcap/data/tcp_router/config/haproxy.conf
