<%=
  def client_cert
    if_p('route_registrar.routing_api.client_cert') do |prop|
      return prop
    end.else do
      if_link('routing_api') do |link|
        return link.p('routing_api.mtls_client_cert')
      end
    end

    return ''
  end

  client_cert
-%>
